const {
    PORT,
    LOCAL_PORT,
    TOKEN_SECRET,
    TOKEN_EXPIRATION_TIME,
    SENDGRID_API_KEY,
    API_HOST_URL,
    EMAIL_FROM_ADDRESS,
    REDIS_HOST,
    REDIS_PORT,
    API_CACHE_EXPIRATION,
    CHECKOUT_COM_PUBLIC_KEY,
    CHECKOUT_COM_SECRET_KEY,
    CUSTOM_OBJECT_CONTAINER_NAME,
} = require("./app")
const { SPACE_ID, ACCESS_TOKEN } = require("./contentful")


module.exports = {
    PORT,
    LOCAL_PORT,
    SPACE_ID,
    ACCESS_TOKEN,
    CTP_PROJECT_KEY,
    CTP_AUTH_URL,
    CTP_CLIENT_ID,
    CTP_CLIENT_SECRET,
    CTP_API_URL,
    TOKEN_SECRET,
    TOKEN_EXPIRATION_TIME,
    SENDGRID_API_KEY,
    API_HOST_URL,
    NODEMAILER_HOST,
    NODEMAILER_USERNAME,
    NODEMAILER_PASSWORD,
    EMAIL_FROM_ADDRESS,
    REDIS_HOST,
    REDIS_PORT,
    API_CACHE_EXPIRATION,
    CHECKOUT_COM_PUBLIC_KEY,
    CHECKOUT_COM_SECRET_KEY,
    CUSTOM_OBJECT_CONTAINER_NAME,
}
